#include <stdio.h>
#include <string.h>

bool search_s(char str[]){
    for(int i=0;i<51;i++){
        if(str)
    }
}

int main(){
    int n;
    char arr[100][51];
    scanf("%d", &n);
    for(int i=0;i<n;i++){
        gets(arr[i]);
    }

    return 0;
}
